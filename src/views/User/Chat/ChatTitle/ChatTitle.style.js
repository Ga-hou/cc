import { createUseStyles } from "react-jss";

export default createUseStyles({
  chatTitle: {
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between"
  },
  finishButton: {
    // height: 40,
    // width: 120
  }
});
