import { createUseStyles } from "react-jss";

export default createUseStyles({
  formInput: {
    width: 240
  },
  addAction: {
    marginBottom: 20
  }
});

export const style = {
  formSelect: {
    width: 240
  }
};
